import React, {Component} from 'react';
import ExternalRoutes from './routes/InternalRoutes';

export default class App extends Component {
  render() {
    return (
      <ExternalRoutes />
    );
  }
}